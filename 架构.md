# 技术架构文档

> **版本**：v1.0  
> **最后更新**：2026-06-18  
> **关联文档**：PRD.md、项目状态.md

---

## 1. 架构总览

```
┌──────────────────────────────────────────────────────┐
│                   移动端 (React Native)                │
│                                                      │
│  ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌───────┐ │
│  │ 资料搜索 │  │ 笔记编辑  │  │  知识库   │  │ 我的  │ │
│  │ Search  │  │  Editor  │  │Knowledge │  │Profile│ │
│  └────┬────┘  └────┬─────┘  └────┬─────┘  └───┬───┘ │
│       │            │             │             │      │
│  ┌────┴────────────┴─────────────┴─────────────┴───┐ │
│  │              本地数据层 (SQLite + LanceDB)        │ │
│  └──────────────────────┬──────────────────────────┘ │
│                         │                             │
│  ┌──────────────────────┴──────────────────────────┐ │
│  │         本地 AI (Ollama - 标签/分类/摘要)         │ │
│  └─────────────────────────────────────────────────┘ │
└──────────────────────────┬───────────────────────────┘
                           │ HTTPS (TLS 1.3)
┌──────────────────────────┴───────────────────────────┐
│                  服务端 (FastAPI)                      │
│                                                      │
│  ┌─────────────────────────────────────────────┐     │
│  │               安全中间件层                     │     │
│  │  ├─ 请求鉴权 (JWT)                           │     │
│  │  ├─ 频率限制                                  │     │
│  │  ├─ 输入校验 (Pydantic)                       │     │
│  │  └─ 日志审计                                  │     │
│  └─────────────────────────────────────────────┘     │
│                                                      │
│  ┌───────────────┐  ┌───────────────────────────┐   │
│  │  资料搜索服务   │  │      AI 调度层             │   │
│  │  ├─ 跨源搜索   │  │  ├─ 云端大模型调用          │   │
│  │  ├─ 内容提取   │  │  │  (Claude/GPT)           │   │
│  │  └─ 质量排序   │  │  ├─ Prompt 管理             │   │
│  └───────────────┘  │  └─ 响应后处理               │   │
│                     └───────────────────────────┘   │
│                                                      │
│              环境变量 (绝不进仓库)                      │
│              ├─ API_KEYS                              │
│              └─ DB_CREDENTIALS                        │
└──────────────────────────────────────────────────────┘
```

---

## 2. 技术栈

### 2.1 前端（移动端）

| 层 | 选型 | 原因 |
|----|------|------|
| 框架 | React Native 0.76+ | 跨平台效率高，生态成熟 |
| 语言 | TypeScript 5.x | 类型安全 |
| 导航 | expo-router 或 react-navigation | 文件路由或声明式导航 |
| 状态管理 | Zustand | 轻量，无 boilerplate |
| 富文本编辑 | react-native-rich-editor 或自研 | 需支持 Markdown 快捷输入 |
| 本地存储 | expo-sqlite + LanceDB | 结构化 + 向量检索 |
| 图标 | lucide-react-native | PRD 已指定 |
| HTTP 客户端 | axios + 拦截器 | 统一鉴权头注入 |

### 2.2 服务端

| 层 | 选型 | 原因 |
|----|------|------|
| 框架 | FastAPI (Python 3.11+) | 异步支持好，Pydantic 校验强 |
| 数据库 | PostgreSQL (云端) | 用户元数据、搜索索引 |
| 缓存 | Redis | 频率限制、临时状态 |
| 任务队列 | Celery + Redis | 长任务异步（笔记生成） |
| 部署 | Docker + Nginx 反代 | 可迁移 |
| 监控 | Sentry + Prometheus | 异常追踪 |

### 2.3 AI 层

| 场景 | 方案 |
|------|------|
| 本地推理 | Ollama + Qwen 2.5 7B / Llama 3.1 8B |
| 云端推理 | Claude Sonnet 4（长文本理解）/ GPT-4o |
| 语音转录 | Whisper (本地部署优先) |
| Embedding | text-embedding-3-small 或 BGE-M3 |

---

## 3. 安全架构（红线）

### 3.1 密钥管理

```
绝对禁令：
├─ API Key 绝不进入前端代码
├─ API Key 绝不出现在 Git 仓库
├─ API Key 绝不出现在客户端 bundle
└─ API Key 绝不以任何形式暴露给客户端

正确做法：
├─ 所有 API Key 存储在服务端环境变量
├─ 服务端通过 .env 文件加载（.env 在 .gitignore 中）
├─ 客户端请求到达服务端后，由服务端拼接 Key 调用第三方
├─ 使用密钥管理服务（如 AWS Secrets Manager）可做升级
```

### 3.2 鉴权与授权

```
请求链路：

客户端                          服务端
  │                               │
  ├─ 用户登录 → 获得 JWT Token ──→ │
  │                               ├─ 验证签名 + 过期时间
  ├─ 请求(Header: Authorization)─→│
  │                               ├─ 校验该用户是否有此操作权限
  │                               ├─ 校验输入是否符合 Pydantic Schema
  │                               ├─ 校验频率限制
  │                               ├─ 拼接 API Key → 调第三方
  │                               ├─ 记录审计日志
  │                               │
  │← ── 响应(仅返回结果，无 Key) ──┘
```

### 3.3 前端数据不可信

```
服务端必须做的：
├─ 所有输入经过 Pydantic 严格校验（类型、范围、长度）
├─ SQL 查询使用参数化，绝不拼接字符串
├─ 文件上传校验 MIME 类型 + 大小限制 + 病毒扫描
├─ 用户只能访问自己的数据（行级权限校验）
├─ 响应数据脱敏（不返回内部 ID、错误堆栈）
```

### 3.4 传输与存储

- 全链路 HTTPS (TLS 1.3)
- 本地 SQLite 数据库文件加密（SQLCipher）
- 云端备份数据 AES-256 加密后上传
- 用户资料仅在笔记生成时按需发送云端，处理完即清除

---

## 4. 目录结构

```
笔记本/                          # 项目根目录
├── PRD.md                       # 产品需求文档
├── 架构.md                      # 本文件
├── 项目状态.md                   # 进度追踪
├── .gitignore                   # Git 忽略规则（含 .env）
├── .env.example                 # 环境变量模板（不含真实 Key）
│
├── mobile/                      # React Native 移动端
│   ├── app/                     # expo-router 页面
│   │   ├── (tabs)/              # 底部 Tab 页面
│   │   │   ├── index.tsx        # 首页（资料搜索）
│   │   │   ├── knowledge.tsx    # 知识库
│   │   │   ├── pending.tsx      # 待学清单
│   │   │   └── profile.tsx      # 我的
│   │   ├── note/                # 笔记相关页面
│   │   │   └── [id].tsx         # 笔记编辑/查看
│   │   └── material/            # 资料相关页面
│   │       └── [id].tsx         # 资料详情/阅读
│   ├── components/              # 通用组件
│   │   ├── ui/                  # 基础 UI 组件
│   │   │   ├── Button.tsx       # 按钮（主/次/文字/危险）
│   │   │   ├── Card.tsx         # 卡片
│   │   │   ├── Tag.tsx          # 标签
│   │   │   ├── Input.tsx        # 输入框
│   │   │   ├── SearchBar.tsx    # 搜索栏
│   │   │   └── Skeleton.tsx     # 骨架屏
│   │   ├── material/            # 资料相关组件
│   │   ├── note/                # 笔记相关组件
│   │   └── knowledge/           # 知识库相关组件
│   ├── stores/                  # Zustand 状态
│   ├── services/                # API 调用层
│   │   └── api.ts               # axios 实例（鉴权头注入）
│   ├── theme/                   # 设计令牌
│   │   ├── colors.ts            # 色彩常量
│   │   ├── typography.ts        # 字体样式
│   │   └── spacing.ts           # 间距常量
│   ├── hooks/                   # 自定义 Hooks
│   ├── utils/                   # 工具函数
│   └── types/                   # TypeScript 类型定义
│
├── server/                      # FastAPI 服务端
│   ├── main.py                  # 应用入口
│   ├── config.py                # 配置管理（从环境变量加载）
│   ├── dependencies.py          # 依赖注入（鉴权、DB连接）
│   ├── routers/                 # API 路由
│   │   ├── auth.py              # 认证接口
│   │   ├── search.py            # 资料搜索接口
│   │   ├── note.py              # 笔记生成接口
│   │   └── knowledge.py         # 知识库接口
│   ├── models/                  # Pydantic 模型
│   │   ├── request.py           # 请求 Schema
│   │   └── response.py          # 响应 Schema
│   ├── services/                # 业务逻辑
│   │   ├── search_service.py    # 搜索服务
│   │   ├── note_service.py      # 笔记生成服务
│   │   ├── ai_service.py        # AI 调用封装（Key 在此拼接）
│   │   └── knowledge_service.py # 知识库服务
│   ├── middleware/              # 中间件
│   │   ├── auth.py              # JWT 鉴权中间件
│   │   ├── ratelimit.py         # 频率限制
│   │   └── logging.py           # 审计日志
│   ├── db/                      # 数据库
│   │   ├── models.py            # SQLAlchemy ORM 模型
│   │   └── migrations/          # Alembic 迁移
│   └── tests/                   # 服务端测试
│
└── docs/                        # 补充文档
    ├── api-spec.md              # API 接口文档
    └── decisions.md             # 技术决策记录
```

---

## 5. 数据模型（核心实体）

```
User
├─ id: UUID
├─ email: string
├─ created_at: datetime

Material (资料)
├─ id: UUID
├─ user_id: FK → User
├─ title: string
├─ source_type: enum(web, paper, video, book, podcast)
├─ source_url: string
├─ content_snapshot: text (缓存正文)
├─ quality_score: float
├─ tags: Tag[]
├─ created_at: datetime

Note (复习笔记)
├─ id: UUID
├─ material_id: FK → Material
├─ user_id: FK → User
├─ title: string
├─ body_json: JSON (富文本结构)
├─ reflection_zone: text (用户自己写的理解)
├─ extension_questions: JSON
├─ cards: JSON (问答卡片)
├─ mindmap: JSON (思维导图数据)
├─ tags: Tag[]
├─ created_at: datetime
├─ updated_at: datetime

Tag
├─ id: UUID
├─ name: string
├─ color: string (可选)
├─ type: enum(auto, manual)

Category (分类夹)
├─ id: UUID
├─ user_id: FK → User
├─ name: string
├─ parent_id: FK → Category (自引用，层级)
├─ materials: Material[]
├─ notes: Note[]
```

---

## 6. API 设计原则

- RESTful 风格，JSON 格式
- 所有接口（除登录注册）需 Authorization Header
- 分页使用 cursor-based（应对大量资料）
- 错误响应统一格式：`{ "error": { "code": "...", "message": "..." } }`
- 长耗时操作（笔记生成）返回 task_id，前端轮询状态

### 核心接口预览

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/login | 登录 |
| POST | /api/search | 资料搜索 |
| GET | /api/materials/:id | 资料详情 |
| POST | /api/notes/generate | 触发笔记生成 |
| GET | /api/notes/:id | 获取笔记 |
| PUT | /api/notes/:id | 更新笔记 |
| GET | /api/knowledge/search | 语义搜索 |
| GET | /api/knowledge/tags | 获取所有标签 |
| POST | /api/knowledge/export | 导出知识条目 |

---

## 7. 前端组件树（核心路径）

```
App
├─ AuthGate（未登录 → 登录页）
│
├─ TabNavigator
│   ├─ HomeScreen（首页/搜索）
│   │   ├─ SearchBar
│   │   ├─ TypeFilterBar（Tab 式类型筛选）
│   │   ├─ MaterialList
│   │   │   └─ MaterialCard（评分、摘要、直达按钮）
│   │   └─ SkeletonList（加载态）
│   │
│   ├─ KnowledgeScreen（知识库）
│   │   ├─ SemanticSearchBar
│   │   ├─ CategoryTree / TagFilter
│   │   └─ KnowledgeList
│   │       └─ KnowledgeCard（标题、标签、来源、日期）
│   │
│   ├─ PendingScreen（待学清单）
│   │   └─ PendingList → MaterialCard
│   │
│   └─ ProfileScreen（我的）
│       ├─ StatsBar（资料/笔记数量统计）
│       ├─ ExportButton
│       └─ SettingsList
│
├─ MaterialDetailScreen（资料阅读）
│   ├─ ContentView（缓存的正文）
│   ├─ SourceLink（外部跳转）
│   └─ GenerateNoteButton（陶土主按钮）
│
└─ NoteEditorScreen（笔记编辑）
    ├─ AIOutlineSection（逻辑链）
    ├─ ConceptsSection（关键概念）
    ├─ ReflectionZone（留白区，可编辑）
    ├─ ExtensionsSection（延伸问题）
    ├─ AuxiliaryTabs（思维导图 / 问答卡片）
    └─ SaveButton（保存）
```

---

## 8. 开发环境搭建

### 前置依赖

- Node.js 20+
- Python 3.11+
- Ollama（本地模型）
- Docker（用于 PostgreSQL、Redis）

### 环境变量（.env.example）

```bash
# 服务端
DATABASE_URL=postgresql://user:pass@localhost:5432/zhiyuan
REDIS_URL=redis://localhost:6379
JWT_SECRET=your-secret-here
OPENAI_API_KEY=sk-xxx          # 仅服务端环境变量
CLAUDE_API_KEY=sk-ant-xxx      # 仅服务端环境变量
OLLAMA_HOST=http://localhost:11434

# 移动端
API_BASE_URL=http://localhost:8000
```
